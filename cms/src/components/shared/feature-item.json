{
  "collectionName": "components_shared_feature_items",
  "info": {
    "displayName": "Feature Item"
  },
  "options": {},
  "attributes": {
    "title": {
      "type": "string",
      "required": true
    },
    "description": {
      "type": "text",
      "required": true
    },
    "link": {
      "type": "component",
      "repeatable": false,
      "component": "shared.cta-link"
    },
    "icon": {
      "type": "enumeration",
      "enum": ["shield", "workflow", "chart", "users", "lock", "spark"],
      "default": "spark"
    }
  }
}
